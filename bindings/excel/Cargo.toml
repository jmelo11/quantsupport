[package]
name = "quantsupport-xll"
version.workspace = true
edition = "2021"
description = "Excel XLL bindings for QuantSupport"
license = "MIT"
repository = "https://github.com/jmelo11/quantsupport"
publish = false

[lib]
name = "quantsupport_xll"
crate-type = ["cdylib"]

[dependencies]
quantsupport = { path = "../.." }
xll-rs = "0.1.1"
