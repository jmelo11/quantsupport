[package]
name = "cva"
version = "0.1.0"
edition = "2021"
description = "CVA + AAD sensitivities example using XvaEvaluator"
license = "MIT"
publish = false

[dependencies]
quantsupport = { path = "../../" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
rayon = "1"
