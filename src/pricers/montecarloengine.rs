// pub type Scenario<T> = Vec<T>;
// pub trait MonteCarloEngine {
//     /// Runs the Monte Carlo simulation and returns the results.
//     fn simulate(&self, scenario: &mut Scenario<f64>);
// }

// pub trait MonteCarloPlanner {
//     /// Plans the Monte Carlo simulation by determining the number of paths and time steps.
//     fn plan(&self) -> (usize, usize);
// }
